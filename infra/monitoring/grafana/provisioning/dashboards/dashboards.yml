apiVersion: 1

providers:
  - name: "default"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
    updateIntervalSeconds: 30
    # You can scope by allowing only specific JSON files:
    # options:
    #   path: /var/lib/grafana/dashboards
    #   foldersFromFilesStructure: false
